# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/
blob-report/

# Meshy asset harvest (issue #25 Stage A) — raw candidates, private dashboard/plan evidence,
# local binary archive. Only accepted+optimized production GLBs (public/assets/models) and the
# text provenance ledger (docs/asset-harvest-log.md) are committed.

# Human Visual Gold Standard v1 (issue #27) — diagnostic/raw Meshy intermediates (regenerated by
# scripts/human-proof/*). Kept OUTSIDE public/ so the production build can never copy them into
# dist/. The committed review asset (dev-review-assets/human_gold_calibration_01.glb) is NOT ignored.
dev-review-assets/_proof/
# Legacy location guard (must stay empty — enforced by productionBundle.test.ts).
public/assets/models/characters/_proof/

# H0 raw diagnostic evidence (working screenshots) — not committed; the curated review package
# lives under docs/review/h0-calibration/ instead.
asset-archive/
